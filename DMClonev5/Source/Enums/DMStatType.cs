using System;

namespace DungeonMaker;

public enum DMStatType
{
    LIFE,
    ATK,
    DEF,
    DEX,
    NumAttacks,
    Combat,
    Speed,
    
    Attack = ATK,
    Defense = DEF,
    Dexterity = DEX
}